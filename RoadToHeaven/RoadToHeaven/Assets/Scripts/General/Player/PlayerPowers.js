#pragma strict

private var PS: PowerSystem;

function Awake()
{
	PS = GameObject.FindGameObjectWithTag("PlayerController").GetComponent(PowerSystem);
}

function AngelPower1()
{
	//Do something
	if(PS.aPower1Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.aPower1Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower1Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower1Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower1Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function AngelPower2()
{
	//Calculate Upgrades from the store!
	if(PS.aPower2Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.aPower2Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower2Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower2Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower2Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function AngelPower3()
{
	//Calculate Upgrades from the store!
	if(PS.aPower3Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.aPower3Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower3Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower3Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower3Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function AngelPower4()
{
	//Calculate Upgrades from the store!
	if(PS.aPower4Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.aPower4Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower4Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower4Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower4Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function AngelPower5()
{
	//Calculate Upgrades from the store!
	if(PS.aPower5Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.aPower5Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower5Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower5Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower5Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function AngelPower6()
{
	//Calculate Upgrades from the store!
	if(PS.aPower6Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.aPower6Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower6Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower6Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower6Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function AngelPower7()
{
	//Calculate Upgrades from the store!
	if(PS.aPower7Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.aPower7Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower7Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower7Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower7Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function AngelPower8()
{
	//Calculate Upgrades from the store!
	if(PS.aPower8Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.aPower8Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower8Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower8Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower8Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function AngelPower9()
{
	//Calculate Upgrades from the store!
	if(PS.aPower9Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.aPower9Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower9Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower9Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower9Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function AngelPower10()
{
	//Calculate Upgrades from the store!
	if(PS.aPower10Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.aPower10Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower10Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower10Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.aPower10Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function DemonPower1()
{
	//Calculate Upgrades from the store!
	if(PS.dPower1Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.dPower1Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower1Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower1Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower1Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function DemonPower2()
{
	//Calculate Upgrades from the store!
	if(PS.dPower2Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.dPower2Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower2Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower2Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower2Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function DemonPower3()
{
	//Calculate Upgrades from the store!
	if(PS.dPower3Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.dPower3Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower3Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower3Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower3Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function DemonPower4()
{
	//Calculate Upgrades from the store!
	if(PS.dPower4Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.dPower4Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower4Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower4Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower4Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function DemonPower5()
{
	//Calculate Upgrades from the store!
	if(PS.dPower5Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.dPower5Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower5Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower5Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower5Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function DemonPower6()
{
	//Calculate Upgrades from the store!
	if(PS.dPower6Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.dPower6Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower6Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower6Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower6Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function DemonPower7()
{
	//Calculate Upgrades from the store!
	if(PS.dPower7Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.dPower7Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower7Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower7Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower7Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function DemonPower8()
{
	//Calculate Upgrades from the store!
	if(PS.dPower8Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.dPower8Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower8Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower8Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower8Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function DemonPower9()
{
	//Calculate Upgrades from the store!
	if(PS.dPower9Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.dPower9Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower9Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower9Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower9Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}

function DemonPower10()
{
	//Calculate Upgrades from the store!
	if(PS.dPower10Ab[0] != 0)
	{
		//change nothing this is the base power!
	}
	
	if(PS.dPower10Ab[1] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower10Ab[2] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower10Ab[3] != 0)
	{
		//Change base vars for this power!
	}
	
	if(PS.dPower10Ab[4] != 0)
	{
		//Change base vars for this power!
	}
	
	//Execute power with new vars
}